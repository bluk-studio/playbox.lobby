package org.playbox.resourcepack.bits;

import org.playbox.utils.ResourcePackBit;
import team.unnamed.creative.file.FileTree;

public class Badges extends ResourcePackBit {
    public void apply(FileTree tree) {

    };
}
